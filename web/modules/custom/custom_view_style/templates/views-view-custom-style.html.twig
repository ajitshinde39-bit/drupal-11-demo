<div class="row">
    {% for item in my_rows %}
    <div class="col-lg-6">
        <div class="service-item">
        <i class="fas {{ item.article_class }}"></i>
        <h4>{{ item.title }}</h4>
        <p>{{ item.body|raw }}</p>
        </div>
    </div>
    {% endfor %}
</div>
